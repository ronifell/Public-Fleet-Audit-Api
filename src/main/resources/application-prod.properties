# Fonte de dados:
spring.datasource.url=${DATASOURCE_URL:jdbc:h2:file:/tmp/seagri;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
spring.datasource.username=${DATASOURCE_USERNAME:sa}
spring.datasource.password=${DATASOURCE_PASSWORD:}

# Faster startup profile for Render
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.main.lazy-initialization=true

# Server:
server.port=${PORT:9000}

# JWT
jwt.secret=6qfR9uV2xW4zK8nP1sT7mY3aC5dH0jL4bN9eQ2rU6iO1pA8fG3kM7tV5yX0zC4
